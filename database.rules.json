{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "sessions": {
      "$code": {
        ".read": "auth != null && data.child('uids').child(auth.uid).exists()",
        ".write": "auth != null && (data.child('uids').child(auth.uid).exists() || !data.exists())",
        "uids": {
          "$uid": {
            ".validate": "auth.uid === $uid"
          }
        }
      }
    },
    "invites": {
      ".read": false,
      ".write": false
    }
  }
}
